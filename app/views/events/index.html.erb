<h1>Events: </h1>
<% @events.each do |event| %>
  <%= render partial: 'event', locals: { event: event } %>
  <p>
    <a href="events/<%= event.id %>">View Event</a>
    <br />
    <br />
  </p>
<% end %>
